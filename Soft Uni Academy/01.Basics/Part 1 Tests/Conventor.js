/*function convertor(input) {
    let usd = Number(input[0])
    let bgn = usd * 1.79549
    consol.log (bgn 
        Treta zadacha otdolo     */

   /* function depositCalculator(input){
        let depositSum = Number(input[0]);
        let depositPeriod = Number(input[1]);
        let annualIP = Number(input[2]);
        let annualID = annualIP / 100;

        let Sum = depositSum + depositPeriod * ((depositSum * annualID) / 12);
        
        console.log (Sum);    

    }

    depositCalculator (["200", "3", "5.7"]); 

    Next: */
    /*
    function Supplies(input) {
        let penPacketsCount = Number(input[0]);
        let markerPacketsCount = Number(input[1]);
        let detergentsLieters = Number(input[2]);
        let discountPercent = Number(input[3]) /100;
        
        let totalPensPrice = penPacketsCount * 5.80;
        let totalMarcersPrice = markerPacketsCount * 7.20;
        let totalDetergentPrice = detergentsLieters *1.20;

        let totalPrice = totalPensPrice + totalMarcersPrice + totalDetergentPrice
        let totalDiscount = discountPercent * totalPrice;
        let PriceAferDiscount = totalPrice - totalDiscount;

        console.log(PriceAferDiscount);
    }

    Supplies (["2","3","4","25"])
   To Do....... */
   /*

  function foodDeliveri(input){
    let chickenMenuCount = Number(input[0]);
    let fishMenuCount = Number(input[1]);
    let veganMenuCout = Number(input[2]);

    let chickenMenuPrice = chickenMenuCount * 10.35;
    let fishMenuPrice = fishMenuCount * 12.40;
    let veganMenuPrice = veganMenuCout * 8.15;

    let totalMenuPrice = chickenMenuPrice + fishMenuPrice + veganMenuPrice
    let dessert =  0.2 * totalMenuPrice 

    let PriceWithDesert = totalMenuPrice + dessert

    let PriceWithDeleivery = PriceWithDesert + 2.50;

    console.log(PriceWithDeleivery);
  }


  foodDeliveri ("2","4","3")
+8.1134843254805*/

/*
     function radiansToDegreece(input){
    let radians = Number(input[0]);
    let derees = ((radians * 180) / Math.PI)  ;

    console.log(derees);

   }
 
   radiansToDegreece("3.1416")
*/


